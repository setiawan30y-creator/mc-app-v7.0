@extends('layouts.app')

@section('content')
<div class="container-fluid py-4">

    @if(session('success'))
        <div class="alert alert-success">{{ session('success') }}</div>
    @endif

    <div class="d-flex justify-content-between align-items-start mb-4">
        <div>
            <h1 class="h3 mb-1">{{ $account->bank_name }}</h1>
            <p class="text-muted mb-1">
                {{ $account->account_name }}
                · {{ $account->account_number }}
            </p>

            @if($account->currency)
                <span class="badge bg-light text-dark border">
                    {{ $account->currency->code }} — {{ $account->currency->name }}
                </span>
            @endif
        </div>

        <div class="d-flex gap-2">
            <a href="{{ route('settings.bank-accounts.edit', $account->id) }}"
               class="btn btn-outline-primary">
                Edit
            </a>

            <a href="{{ route('settings.bank-accounts.mutations.index', $account->id) }}"
               class="btn btn-primary">
                Mutasi Bank
            </a>
        </div>
    </div>

    <div class="row g-3 mb-4">

        <div class="col-md-4">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <div class="small text-muted mb-1">Saldo Awal</div>
                    <div class="fs-4 fw-bold">
                        {{ number_format((float)$account->opening_balance, 2, ',', '.') }}
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <div class="small text-muted mb-1">Saldo Berjalan</div>
                    <div class="fs-4 fw-bold">
                        {{ number_format((float)$calculatedBalance, 2, ',', '.') }}
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body">
                    <div class="small text-muted mb-1">Status</div>

                    @if($account->is_active)
                        <span class="badge bg-success fs-6">Aktif</span>
                    @else
                        <span class="badge bg-secondary fs-6">Nonaktif</span>
                    @endif
                </div>
            </div>
        </div>

    </div>

    <div class="card shadow-sm border-0">
        <div class="card-header bg-white">
            <strong>Informasi Rekening</strong>
        </div>

        <div class="card-body">
            <dl class="row mb-0">

                <dt class="col-sm-3">Bank</dt>
                <dd class="col-sm-9">
                    {{ $account->bank_name }}
                    @if($account->bank_code)
                        ({{ $account->bank_code }})
                    @endif
                </dd>

                <dt class="col-sm-3">Nama Rekening</dt>
                <dd class="col-sm-9">{{ $account->account_name }}</dd>

                <dt class="col-sm-3">Nomor Rekening</dt>
                <dd class="col-sm-9"><code>{{ $account->account_number }}</code></dd>

                <dt class="col-sm-3">Mata Uang</dt>
                <dd class="col-sm-9">
                    {{ $account->currency?->code ?? '-' }}
                </dd>

                <dt class="col-sm-3">Catatan</dt>
                <dd class="col-sm-9">
                    {{ $account->notes ?: '-' }}
                </dd>

            </dl>
        </div>
    </div>

    <div class="mt-3">
        <a href="{{ route('settings.bank-accounts.index') }}"
           class="btn btn-outline-secondary">
            ← Kembali ke Rekening Bank
        </a>
    </div>

</div>
@endsection
